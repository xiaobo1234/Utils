<resources xmlns:android="http://schemas.android.com/apk/res/android">

    <style name="MyAppTheme" parent="@android:style/Theme.Light.NoTitleBar">
        <item name="android:splitMotionEvents">false</item>
    </style>

    <style name="AnimAlpha" parent="@android:style/Animation">
        <item name="android:windowEnterAnimation">@anim/image_alpha_in</item>
        <item name="android:windowExitAnimation">@anim/image_alpha_out</item>
    </style>

    <style name="DialogFullScreen">
        <item name="android:background">@android:color/transparent</item>
        <item name="android:windowBackground">@android:color/transparent</item>
        <item name="android:backgroundDimEnabled">false</item>
        <item name="android:windowNoTitle">true</item>
        <item name="android:windowAnimationStyle">@style/AnimAlpha</item>
        <item name="android:backgroundDimAmount">0.5</item>
    </style>

</resources>
